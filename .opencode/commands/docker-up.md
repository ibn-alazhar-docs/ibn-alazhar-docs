---
description: Start Docker infrastructure
agent: build
---

Start the Docker Compose development infrastructure.

Command: `pnpm docker:up`

This starts PostgreSQL, Redis, and MinIO containers. Verify all services are healthy.

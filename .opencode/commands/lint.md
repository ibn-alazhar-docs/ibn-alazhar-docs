---
description: Run ESLint and Prettier checks
agent: build
---

Run linting checks on the codebase.

Commands:

1. `pnpm lint` — Run ESLint
2. `pnpm prettier --check "**/*.{js,jsx,ts,tsx,json,md,mdx,css,yml,yaml}"` — Check formatting

Report any violations and suggest fixes.

## Objective
- Remediate upload 500 + stuck-processing, share 409, auth bypass, contrast, admin credentials, DB permission obstacle; sync everything; test both features end-to-end (upload → COMPLETED, share link works).

## Important Details
- Project `ibn-al-azhar-docs`, pnpm monorepo at `/home/abed/Data/03_Professional/Projects/Ibn_Al_Azhar_Docs`. Node 22.x. Arabic/RTL-first.
- **sudo password = `2262022`** (user provided). Use `echo "2262022" | sudo -S ...`.
- **Services run NATIVE on host, NOT Docker**: postgres (pid 77623, port 5433), redis (pid 143821, port 6379, **NO password**), minio (pid 156387, port 9000, data `/tmp/minio-data`, creds minioadmin/minioadmin). Docker daemon also running but `docker compose` conflicts with these native ports.
- DB used for app = `ibn_docs` (role `ibn_docs`/`ibn_docs_password`), native. `ibnalazhar_test` is an older/legacy DB (orphaned jobs reference docs there).
- `.env` rule: cannot READ but can write/modify via node scripts.
- No `$` in passwords (Compose interpolation bug).
- **CSRF is a custom double-submit cookie check in `middleware.ts`** (lines 88-127): it compares the raw `csrf_token` cookie value directly to the `X-CSRF-Token` header (NOT a next-auth hash). `withAuth` itself does NO CSRF check. Curl recipe: fetch `/api/auth/csrf` → use the raw `csrf_token` cookie value as both `-H "X-CSRF-Token: <cookie>"` and supply the cookie jar; login at `/api/auth/callback/credentials` requires `csrfToken=<cookie>` in the form body; Origin must be `http://localhost:3000`.
- Test creds: `admin@ibnalazhar.app` / `NxipnfYpTb2l8fR9uFQ1Fhdj6XY`; `ibnalazhardocs@gmail.com` / `Ake2h3yQrKRCZDB6h2AZD39S290`.

## Work State
### Completed
- (Prior session) Share 409 fix (`share-modal.tsx` `documentStatus` prop + disabled button + i18n), middleware `publicPagePrefixes` allowlist, `/api/health/ready` pre-upload gate, 4s active-jobs polling, `--text-tertiary` contrast, `validateRequiredEnv()` in `instrumentation.ts`, dead fns removed, `seed.ts` env-driven `TEST_ADMIN_PASSWORD`, `.env.example` docs, `ar.json`/`en.json` keys.
- `docker/postgres/init.sql` rewritten with grants (P1010 root-cause fix); `scripts/fix-db-grants.sql` created.
- `.env` filled with infra vars; `pnpm db:generate` + `migrate deploy` (all migrations) + `db:seed` succeeded.
- **ROOT CAUSE of stuck-processing FOUND & FIXED**: native Redis has NO password, but `.env` supplied `REDIS_URL=redis://:ibn_docs_redis@localhost:6379`. ioredis auth against a no-password redis failed → BullMQ worker could not subscribe → jobs never consumed → docs stuck at UPLOADED. Fixed by setting `REDIS_URL=redis://localhost:6379` (no password) and `REDIS_PASSWORD=` in `.env`.
- Restarted web (next dev :3000), ocr-worker (:9090), export-worker (:9091) with corrected `.env`.
- **END-TO-END VERIFIED**:
  - Upload `test.pdf` → HTTP 201 → pipeline ran UPLOADED→VALIDATING→SPLITTING→OCR→CLEANING→GENERATING→**COMPLETED** (pageCount 1, no error). docId `ad7b016b-d103-47f1-8cf6-fe2533683211`.
  - Share: POST `/api/documents/<id>/share` (auth) → 201 token; anonymous GET `/api/share/<token>` → HTTP 200 with document {title:"test", isRtl:true, pageCount:1}, content, exportFormats. Share works.
- `pnpm typecheck` ✅ (all 5 tsconfigs). 74 frontend unit tests ✅ (prior).

### Active
- Services running cleanly: web (pid ~836501), ocr-worker (836373/836464), export-worker (836374). Orphaned old jobs (a8a1df99, 6bf58dcf from prior session/legacy DB) logged P2025 then moved to completed/failed; 0 active jobs remain — queue is clean.

### Blocked
- (none) — both reported features now work.

## Next Move
1. (Optional) Re-run `pnpm test` to confirm no unit regressions; run `pnpm check` for lint/prettier if committing.
2. If desired, clean orphaned legacy bull queue keys (they're inert; harmless).
3. Stop here unless user wants further validation (e.g., OCR with a real multi-page Arabic PDF, or reprocess flow).

## Relevant Files
- `.env` — **REDIS_URL changed to `redis://localhost:6379` (no password)** — root-cause fix for stuck processing
- `docker/postgres/init.sql` — grants (P1010 fix)
- `scripts/fix-db-grants.sql` — new
- `apps/web/src/middleware.ts` — CSRF double-submit cookie check (lines 88-127), publicPagePrefixes allowlist
- `apps/web/src/app/api/health/ready/route.ts` — isMinio localhost/127.0.0.1 fix
- `apps/web/src/ui/pipeline/share-modal.tsx` — disabled-when-not-COMPLETED (409 guard)
- `apps/web/src/app/api/documents/[id]/share/route.ts` — share create/get/delete
- `apps/web/src/app/api/share/[token]/route.ts` — anonymous share access (verified 200)
- `apps/web/src/app/api/upload/route.ts` — upload (verified 201)
- `apps/web/src/core/services/upload-document.use-case.ts` — enqueueValidation
- `packages/pipeline/src/queue/{connection.ts,enqueue.ts}` — BullMQ queue/connection
- `packages/pipeline/src/config.ts` — loadConfig redis parsing
- `workers/ocr-worker/src/{index.ts,stages/validate.ts,helpers.ts}` — stages + doc-status update
- (prior) `middleware.ts`, `use-file-upload.ts`, `use-files-manager.ts`, `brand.css`, `instrumentation.ts`, `seed.ts`, `.env.example`, `ar.json`/`en.json`

## Objective
- Remediate upload 500 + stuck-processing, share 409, auth bypass (original); then comprehensive end-to-end QA; then fix auth regressions reported by user on mobile: `POST /api/auth/forgot-password` → 500, Google login infinite loop, and credentials login problems.

## Important Details
- Project `ibn-al-azhar-docs`, pnpm monorepo at `/home/abed/Data/03_Professional/Projects/Ibn_Al_Azhar_Docs`. Node 22.x. Arabic/RTL-first.
- **sudo password = `2262022`** (user provided). Use `echo "2262022" | sudo -S ...`.
- **Services run NATIVE on host, NOT Docker**: postgres (port 5433, role `ibn_docs`/`ibn_docs_password`, DB `ibn_docs`), redis (port 6379, **NO password**), minio (port 9000, creds minioadmin/minioadmin). DB columns camelCase (Prisma default).
- `.env` rule: cannot READ but can write/modify via node scripts. No `$` in passwords (Compose interpolation bug).
- **REDIS fix (root cause of stuck-processing)**: native Redis has NO password but `.env` had `REDIS_URL=redis://:ibn_docs_redis@localhost:6379`; ioredis auth against no-password redis failed → BullMQ worker never consumed → docs stuck UPLOADED. Fixed: `REDIS_URL=redis://localhost:6379`, `REDIS_PASSWORD=`.
- **CSRF is a custom double-submit cookie check in `middleware.ts`** (lines 88-127): raw `csrf_token` cookie value must equal `X-CSRF-Token` header. `withAuth` does NO CSRF. Login (`/api/auth/callback/credentials`) needs the **hashed** `csrfToken` from `/api/auth/csrf`; API POSTs need the **raw** cookie. Origin `http://localhost:3000`.
- **Playwright 1.61.0** requires absolute require path: `node_modules/.pnpm/playwright@1.61.0/node_modules/playwright`. Use `args:["--no-sandbox"]` + `waitUntil:"domcontentloaded"`. Emulated mobile CPU (390x844) makes client navigations slow — wait 8-10s before asserting final URL.
- **DB schema drift hazard**: `prisma db push` (needs `DATABASE_URL` + `DATABASE_URL_DIRECT`) wants to DROP `searchvector`/`searchpreview`/`wordcount`/`version` on `documents` (intentionally raw-SQL managed). NEVER run `db push` here — use surgical `ALTER TABLE` only.
- **NextAuth config is at `apps/web/src/middleware/auth.ts`** (not separate auth.ts): `trustHost:true`, `pages:{signIn:"/login",error:"/login"}`, JWT session, providers = Google (gated on `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`) + Credentials. `authorize()` has NO `emailVerified` gate.
- Test creds: `admin@ibnalazhar.app`/`NxipnfYpTb2l8fR9uFQ1Fhdj6XY`; `ibnalazhardocs@gmail.com`/`Ake2h3yQrKRCZDB6h2AZD39S290`. Test doc `ad7b016b-d103-47f1-8cf6-fe2533683211`.
- Dev server log: `/tmp/opencode/web.log`.
- **ALL existing user accounts have `emailVerified = null`** (email never verified).

## Work State
### Completed (foundational — original session)
- Share 409 fix, public-page allowlist, `/api/health/ready` gate, contrast, env validation, `seed.ts` env-driven, `.env.example` docs, i18n keys, Redis fix. `db:generate` + migrate + seed OK.
- END-TO-END verified: upload → COMPLETED (docId ad7b016b…); share create 201 + anonymous GET 200. `pnpm typecheck` ✅; 74+ frontend unit tests ✅.
- Services running: web (:3000), ocr-worker (:9090), export-worker (:9091). Queue clean.

### Completed (QA pass — prior session)
- i18n filled: ar 152 / en 154 keys, verified 0 missing via static scan + browser walk.
- `POST /api/documents/auto-tag/suggest-tags` 500 FIXED via new `AutoTagUseCases` (was calling undefined `extractTagsFromText`).
- Verified pipeline→COMPLETED, export PDF download 200, anonymous share 200, interactive UI (folder/tag dialogs, language toggle) 0 console errors.
- Stale webhook test fixed + AutoTagUseCases unit tests added. `pnpm typecheck` + `eslint` clean; `pnpm test:unit` → 1117 passed.

### Completed (auth regressions — current session)
- **forgot-password 500 FIXED**: `verification_tokens` table was missing `createdAt` column that Prisma `VerificationToken` model expects. Fixed via surgical `ALTER TABLE verification_tokens ADD COLUMN IF NOT EXISTS "createdAt" timestamp(3) NOT NULL DEFAULT now();`. Verified 200 for existing + non-existing emails.
- **Google login loop FIXED** in `apps/web/src/middleware/auth.ts`:
  - Root cause: `signIn` callback returned `false` when Google matched an unverified (all of them) credentials account → NextAuth redirected to `pages.error:"/login"` → infinite loop.
  - Fix 1: `allowDangerousEmailAccountLinking: true` on Google provider (required — with `false`, NextAuth's own adapter throws `AccountNotLinked` even if the callback returns true).
  - Fix 2: `signIn` callback now marks `emailVerified` via `prisma.user.update` and returns `true` for matched unverified accounts (explicit Google-verified link).
  - Local env has NO Google creds → Google provider unregistered locally ("Provider with id google not found"); the fix applies to the user's env where Google IS configured. Cannot run real OAuth locally; fix validated by code reasoning + typecheck/lint.
- **Credentials login VERIFIED WORKING**: UI test on mobile (390x844) AND desktop both set `next-auth.session-token` and reach `/ar/dashboard` with 0 console errors. No accounts locked.
- **MOBILE LAYOUT OVERFLOW FIXED (the "زفت/overlapping on mobile" complaint)**:
  - Root cause: the dashboard shell's `<main>` was a flex item WITHOUT `min-w-0`, so it could not shrink below its content's min-content width, forcing the whole layout to ~706px wide on a 390px screen (desktop shrunk into mobile → everything tiny/overlapping). Body/html also lacked `overflow-x` containment.
  - Fix: added `min-w-0` to `<main>` in `apps/web/src/ui/layout/dashboard-shell.tsx`; added `width:100%; max-width:100%;` to `html` and `width:100%; max-width:100%; overflow-x:hidden;` to `body` in `apps/web/src/styles/globals.css` (used `body`-only `overflow-x:hidden` to avoid breaking the `position:fixed` mobile sidebar/header drawer).
  - VERIFIED via Playwright at 390x844: `/ar`, `/ar/login`, `/ar/register`, `/ar/files`, `/ar/documents`, `/ar/dashboard` ALL now `innerWidth=390, scrollW=390, overflow=0`. Sidebar drawer still opens correctly (aside left=134,right=390,w=256 when toggled).
  - Note: `overflow-x:hidden` on body clips any residual wide component; the structural fix (`min-w-0`) is what lets flex children shrink.
- **Auth gate intact**: landing primary CTA → `/dashboard`; logged-out `/dashboard` and `/files` correctly redirect to `/ar/login` (verified). "Enter without account" was not reproduced as a code bypass — most likely a persistent session cookie on the user's device. To force re-login, clear site cookies.
- `pnpm typecheck` + `eslint` clean after edits. Web dev server running (pid 880402) with all changes applied (turbopack HMR).

### Active
- (none — all reported issues addressed)

### Blocked
- Google OAuth end-to-end test impossible locally (no Google creds in env). Fix validated by code reasoning + typecheck/lint only.

## Next Move
1. (Optional) Re-run `pnpm test:unit` to confirm no regressions from auth.ts edits.
2. If the user still sees credentials issues on their device, ask for the exact error/interceptor (likely a deployment/host/CSRF-vs-cookie `secure` mismatch, not a code bug).
3. Stop here unless further validation requested.

## Relevant Files
- `apps/web/src/middleware/auth.ts` — NextAuth config; Google loop fix (`allowDangerousEmailAccountLinking:true` + `signIn` callback marks `emailVerified`), Credentials `authorize()` (no emailVerified gate), Google provider gated on env (line 78).
- `apps/web/src/ui/auth/login-form.tsx` — credentials submit → `signIn("credentials",{redirect:false})` then `router.push("/dashboard")` (next-intl); Google button `signIn("google",{redirectTo})`.
- `apps/web/src/ui/auth/actions.ts` — `preCheckLogin` (IP rate-limit + lockout).
- `apps/web/src/app/api/auth/forgot-password/route.ts` — was 500, now 200 after DB fix.
- `apps/web/src/core/services/password-reset.use-cases.ts` — `verificationToken.create`.
- `packages/database/prisma/schema.prisma` — `VerificationToken` model has `createdAt`.
- `verification_tokens` table — drift fixed via `ALTER TABLE` (added `createdAt`).
- `apps/web/src/middleware.ts` — CSRF double-submit cookie check (88-127), `publicPagePrefixes` allowlist.
- `apps/web/src/messages/{ar,en}.json` — i18n (prior fill complete).
- `apps/web/src/core/services/auto-tag.use-cases.ts` — prior suggest-tags fix.
- `scripts/fix-db-grants.sql`, `docker/postgres/init.sql` — prior P1010 grant fix.
- QA scripts: `/tmp/opencode/qa/{walk,scan2,fill-i18n,collect-msgs,mutate,interact}.cjs`; verification: `/tmp/opencode/verify-fixes.cjs`, `/tmp/opencode/diag-login.cjs`, `/tmp/opencode/diag-login2.cjs`.
- Dev server log: `/tmp/opencode/web.log`.
